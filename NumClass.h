

/**
 * A palindrome number is  sequence of numbers that reads the same backwards as forwards.
*/
int isPalindrome(int numInt);



/**
 * Strong number is a special number whose sum of the factorial of digits is equal to the original number. 
 * For Example: 145 is strong number. Since, 1! + 4! + 5! = 145. 

*/
int isArmstrong(int numInt);



/**
 * retuer 0 if numInt is armstrong
*/
int isStrong(int numInt);




/**
 * return 0 if numIint is prime
*/
int isPrime(int numInt);
